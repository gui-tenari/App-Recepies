const INITIAL_STATE = {
  meals: [],
  favoriteMeals: [],
};

const mealsReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default mealsReducer;
